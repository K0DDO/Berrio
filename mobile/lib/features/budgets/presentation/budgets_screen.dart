import '../../../shared/widgets/placeholder_screen.dart';

class BudgetsScreen extends PlaceholderScreen {
  const BudgetsScreen({super.key})
      : super(
          title: 'Budgets',
          subtitle: 'Category & overall budgets — Stage 5.',
        );
}
