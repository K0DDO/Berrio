import '../../../shared/widgets/placeholder_screen.dart';

class AnalyticsScreen extends PlaceholderScreen {
  const AnalyticsScreen({super.key})
      : super(
          title: 'Analytics',
          subtitle: 'Day / week / month / year charts — Stage 5.',
        );
}
