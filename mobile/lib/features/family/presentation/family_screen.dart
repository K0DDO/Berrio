import '../../../shared/widgets/placeholder_screen.dart';

class FamilyScreen extends PlaceholderScreen {
  const FamilyScreen({super.key})
      : super(
          title: 'Family',
          subtitle: 'Roles + flexible permissions — Stage 8.',
        );
}
