import '../../../shared/widgets/placeholder_screen.dart';

class GoalsScreen extends PlaceholderScreen {
  const GoalsScreen({super.key})
      : super(
          title: 'Goals',
          subtitle: 'Financial goals shelf — CRUD later. Architecture ready.',
        );
}
