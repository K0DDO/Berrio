import '../../../shared/widgets/placeholder_screen.dart';

class FinancialHealthScreen extends PlaceholderScreen {
  const FinancialHealthScreen({super.key})
      : super(
          title: 'Berrio Score',
          subtitle: 'Financial health 0–100 with explainable factors — Stage 5.',
        );
}
